include "root" {
  path = find_in_parent_folders()
}

# Placeholder — ArgoCD manages the API service deployment via Helm
# This file can be used for API-specific infrastructure (e.g., DB migrations job)
